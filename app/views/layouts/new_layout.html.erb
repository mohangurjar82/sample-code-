<!DOCTYPE html>
<html lang="en">
  <head>
     <meta charset="UTF-8" />
     <meta content="initial-scale=1.0,maximum-scale=1.0,user-scalable=no" name="viewport" />
     <meta content="IE=edge" http-equiv="X-UA-Compatible" />
     <meta content="no" name="msapplication-tap-highlight" />
     <meta name="viewport" content="width=device-width, initial-scale=1">

     <%= csrf_meta_tags %>
     <title>N2me</title>
     <%= stylesheet_link_tag 'new_layout', media: 'all' %>
     <%= content_for(:head) if content_for? :head %>
     <%= javascript_include_tag(:new_layout) unless @skip_javascript %>
  </head>
  <body>
    <div id="wrapper">
      <% if current_user %>
        <%= render 'layouts/new/navbar' %>
      <% else %>
        <%= render 'layouts/new/navbar_no_login' %>
      <% end %>
      <%= render 'layouts/new/breadcrumbs' %>
      <!--Main Content Start-->
      <div id="cp-main-content">
        <section class="cp-section pd-tb60">
          <%= yield %>
        </section>
        <%= render 'layouts/new/footer' %>
      </div>
    </div>
    <% if content_for? :javascript %>
      <%= content_for :javascript %>
    <% end %>
  </body>
</html>